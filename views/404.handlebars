<div class="centered">
    <h1>404 Oh Noes!</h1>

    <p>
    We couldn't find <code>{{path}}</code>, but maybe you'll like this instead.
    </p>

    {{#puppy}}
        <p>
            <a href="{{url}}" title="{{title}}" class="puppy">
                <img class="puppy-image" src="/img/{{id}}" alt="{{imageAlt}}">
            </a>
        </p>
    {{/puppy}}
</div>
